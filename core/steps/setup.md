# Setup step

1. Locate `workspace/config.yaml` and `workspace/profile.md`.
2. Create missing artifacts from templates without inventing values.
3. Select one active project and inspect its configured source repository read-only.
4. Record each product fact with source path and verification date.
5. Mark unresolved facts as questions and finish with the next smallest setup action.

