interface:
  display_name: "Prepare Call"
  short_description: "Prepare one focused customer conversation"
  default_prompt: "Use $prepare-call to prepare this real validation, discovery, demo, or pilot call."
