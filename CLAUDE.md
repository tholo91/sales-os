# Claude adapter

Follow `AGENTS.md`. Discover canonical skills under `.agents/skills/`. Start with `sales-next` unless setup context is missing. Do not duplicate or reinterpret the workflows in this adapter.

