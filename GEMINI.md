# Gemini adapter

Follow `AGENTS.md`. Load canonical skills from `.agents/skills/` when the host does not discover them automatically. Start with `sales-next` unless setup context is missing. Do not duplicate workflow logic here.

