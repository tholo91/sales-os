# Gemini adapter

Follow `AGENTS.md`. Load canonical skills from `.agents/skills/` when the host does not discover them automatically. Start with `sales-copilot`; it routes setup and specialist phases internally. Do not duplicate workflow logic here.
